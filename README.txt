Steps to running program:

1) Insure that name files are stored in a folder called "input" in the target directory.

2) In proper directory, run command "make multi-lookup"

3) Then run command, "./multi-lookup <num_requestor_threads> <num_resolver_threads> <requestor_log_file> <results_log_file> <name_files>"

